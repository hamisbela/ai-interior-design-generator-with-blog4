const siteConfig = {
  title: "AI Interior Design Generator",
  description: "Create beautiful interior designs with AI technology",
  defaultAuthor: "Interior Design Team",
  // Get the site URL from environment variable or use a fallback
  siteUrl: import.meta.env.VITE_SITE_URL || "https://aiinteriordesigngenerator.com", 
  // Add more site-wide configuration as needed
};

export default siteConfig;